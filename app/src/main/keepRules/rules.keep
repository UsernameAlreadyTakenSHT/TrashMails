# R8 keep rules for the release build (AGP merges every file in src/main/keepRules).
# Nothing to keep: no reflection, no JavaScript interface, JSON is parsed by hand
# (org.json) and OkHttp ships its own consumer rules.
